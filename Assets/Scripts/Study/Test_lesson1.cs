using UnityEngine;

namespace Study
{
    [AddComponentMenu("Lesson1/Test_lesson1 #A")]
    public class Test_lesson1 : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
